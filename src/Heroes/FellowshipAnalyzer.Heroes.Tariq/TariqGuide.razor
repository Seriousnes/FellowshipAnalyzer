@inherits ReportComponent<TariqCombatLogParser>

@if (Parser.FuryEconomyAnalyzers.Count > 0)
{
    <FuryEconomyGuide />
}

@if (Parser.ThunderCallAnalyzers.Count > 0)
{
    <EmpowermentGuide />
}

@if (Parser.FocusedWrathAnalyzers.Count > 0)
{
    <FocusedWrathGuide />
}

@if (Parser.HammerStormAnalyzers.Count > 0)
{
    <HammerStormGuide />
}

@if (Parser.CullingStrikeAnalyzers.Count > 0)
{
    <CullingStrikeGuide />
}
