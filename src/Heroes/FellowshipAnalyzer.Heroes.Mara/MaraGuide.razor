@using FellowshipAnalyzer.Heroes.Mara.Analysis
@using FellowshipAnalyzer.Heroes.Mara.Guides
@inject MaraCombatLogParser Parser

@if (Parser.MaraResourceDisciplineAnalyzers.Count > 0)
{
    <MaraResourceDisciplineGuide />
}

@if (Parser.MaraDotAnalyzers.Count > 0)
{
    <MaraDotGuide />
}

@if (Parser.MaidenOfDeathWindowAnalyzers.Count > 0)
{
    <MaidenOfDeathGuide />
}
