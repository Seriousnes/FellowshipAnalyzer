@inherits Analyzer

@code {
    /// <inheritdoc/>
    public override RenderFragment? Statistic => Procs > 0 ? Card : null;

    private RenderFragment Card =>
        @<StatCard>
            <Header>
                <SpellLink Spell="Talents.SwordAndBoard" />
            </Header>
            <Info>
                <p>
                    The damage below is what @FreeCasts free
                    <SpellLink Spell="Spells.ShieldSlam" /> casts dealt, cleave included. A refund advances the
                    charge count without restarting the recharge already running, so a free cast is worth a whole
                    Shield Slam however close the next one was.
                </p>
            </Info>
            <ChildContent>
                <StatRow Value="@($"{ExtraDamage:N0}")" Tooltip="@DamageTooltip">
                    extra damage from free casts
                </StatRow>
                @if (ProcsExpired > 0)
                {
                    <HelperText>
                        @ProcsExpired of @Procs procs fell off before a Shield Slam was pressed under them.
                    </HelperText>
                }
            </ChildContent>
        </StatCard>;

    private string DamageTooltip =>
        $"Every Shield Slam hit landed between a free cast and the next Shield Slam, across {FreeCasts} " +
        $"free casts from {Procs} procs.";
}
