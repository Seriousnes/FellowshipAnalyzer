@inject ElarionCombatLogParser Parser

@if (Parser.CooldownPairingAnalyzers.Count > 0)
{
    <CooldownPairingGuide />
}

@if (Parser.FocusEconomyAnalyzers.Count > 0)
{
    <FocusEconomyGuide />
}

@if (Parser.LunarlightMarkAnalyzers.Count > 0)
{
    <LunarlightMarkGuide />
}

@if (Parser.CelestialImpetusAnalyzers.Count > 0)
{
    <CelestialImpetusGuide />
}

@if (Parser.SupremacyWindowAnalyzers.Count > 0)
{
    <SupremacyWindowsGuide />
}
