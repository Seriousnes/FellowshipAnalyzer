@inherits ReportComponent<ElarionCombatLogParser>

@if (Parser.CooldownPairingAnalyzers.Count > 0)
{
    <CooldownPairingGuide />
}

@if (Parser.FocusEconomyAnalyzers.Count > 0)
{
    <FocusEconomyGuide />
}

@if (Parser.SupremacyAnalyzers.Count > 0)
{
    <SupremacyGuide />
}
