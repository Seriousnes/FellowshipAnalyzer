@inject ArdeosCombatLogParser Parser

@if (Parser.WildfireComboAnalyzers.Count > 0)
{
    <WildfireComboGuide />
}

@if (Parser.EngulfingFlamesEconomyAnalyzers.Count > 0)
{
    <EngulfingFlamesEconomyGuide />
}

@if (Parser.SearingBlazeAnalyzers.Count > 0)
{
    <SearingBlazeGuide />
}
