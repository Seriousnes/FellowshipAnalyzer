@inject ArdeosCombatLogParser Parser

@if (Parser.WildfireComboReports.Count > 0)
{
    <WildfireComboGuide />
}

@if (Parser.EngulfingFlamesEconomyReports.Count > 0)
{
    <EngulfingFlamesEconomyGuide />
}

@if (Parser.SearingBlazeSpreadReports.Count > 0)
{
    <SearingBlazeSpreadGuide />
}
