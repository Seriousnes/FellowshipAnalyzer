query GetCharacterReports($id: Int!) {
  characterData {
    character(id: $id) {
      id
      name
      canonicalID
      recentReports {
        data {
          code
          title
          startTime
          endTime
          fights {
            id
          }
        }
      }
    }
  }
}
