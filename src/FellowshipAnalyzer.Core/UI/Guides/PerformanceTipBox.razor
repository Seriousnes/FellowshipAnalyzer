<TipBox Variant="@_variant" Title="@Title" HideIcon="@HideIcon">
    <Icon>
        @if (Icon is not null)
        {
            @Icon
        }
        else if (!HideIcon)
        {
            <PerformanceMark Performance="@Performance" Size="14" />
        }
    </Icon>
    <ChildContent>
        @ChildContent
    </ChildContent>
</TipBox>

@code {
    [Parameter, EditorRequired] public QualitativePerformance Performance { get; set; }
    [Parameter] public string? Title { get; set; }
    [Parameter] public RenderFragment? Icon { get; set; }
    [Parameter] public bool HideIcon { get; set; }
    [Parameter] public RenderFragment? ChildContent { get; set; }

    private TipBoxVariant _variant => Performance switch
    {
        QualitativePerformance.Perfect => TipBoxVariant.Info,
        QualitativePerformance.Good => TipBoxVariant.Success,
        QualitativePerformance.Ok => TipBoxVariant.Warning,
        QualitativePerformance.Fail => TipBoxVariant.Error,
        _ => TipBoxVariant.Note,
    };
}
