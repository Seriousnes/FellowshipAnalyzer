@* GuideContainer — root flex-column wrapper used at the top of any guide. *@

<div class="guide-container">
    @ChildContent
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
